<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/stylesheets/favourite.css">
    <title>Beers you like</title>
  </head>
  <body>


    <h1>Your Favourites: </h1>
    <div class="content">

    <% @favourites.each do |fav| %>
      <img src="<%= fav.beer.image_url %>">

      <h3>
        "<%= fav.beer.name %>"
        <p>by</p>
        <%= fav.beer.company %>
      </h3>

      <form action="/beers/<%= fav.id %>" method="post">
        <input type ="hidden" name="_method" value="delete">
        <button>Delete Beer</button>
      </form>
    <% end %>

  </div>

    <a href="/beers">back to home</a>
  </body>
</html>
